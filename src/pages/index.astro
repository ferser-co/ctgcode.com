---
// Raíz del sitio = español (idioma por defecto). Sin redirección: es contenido
// real e indexable. Los demás idiomas viven bajo /<lang> (p. ej. /en).
import Home from "../components/pages/Home/Home.astro";
---

<Home lang="es" />
